import React from "react";
import spinner from "../design/spinner.gif";

const Spinner = () => {
  return (
    <div className="spinner">
      <img src={spinner} alt="" />
    </div>
  );
};

export default Spinner;
